export const MAP_CONFIG = {
  zoom: 6,
  center: [40.0, -3.5]
};

export function initMap(ciudades, onCitySelect) {
  // TODO: Initialize Leaflet map on #map using MAP_CONFIG.center and MAP_CONFIG.zoom.
  // TODO: Iterate ciudades, add a marker for each using ciudad.lat and ciudad.lon.
  // TODO: On marker click, call onCitySelect(ciudad) with the corresponding city object.
}
